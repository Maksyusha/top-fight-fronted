server {
    listen 80; # принимаем запросы с 80 порта
    server_name localhost; # указываем название домена

    root /usr/share/nginx/html;
    index index.html index.htm;

    location / {
        try_files $uri $uri/ /index.html =404;
    }
}
